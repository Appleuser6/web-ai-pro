spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tlias
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 614654
  application:
    name: web-ai-project
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
      enabled: true
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    #驼峰命名开关
    map-underscore-to-camel-case: true
aliyun:
  oss:
    endpoint: https://oss-cn-beijing.aliyuncs.com
    bucketName: java-web-ai-zjx
    region: cn-beijing

logging:
  level:
    com:
      itheima:
        mapper: debug
